import React, { Component } from 'react';
import {
    Image, Text
} from 'react-native';
import {Content} from 'native-base';

export default class Piutang extends Component {
    render() {
        return (
            <Content style={{}}>
                <Text>Piutang Page</Text>
            </Content>
        );
    }
}

module.export = Piutang;